[project]
name = "agent-room-plugin"
version = "1.0.0"
description = "Self-contained shared-room console, owner, and deterministic peer-client plugin for protoAgent."
requires-python = ">=3.11"
license = "MIT"

[tool.hatch.build]
include = ["web/*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E402", "E501", "E702", "E731", "E741"]

[tool.ruff.format]
quote-style = "double"
